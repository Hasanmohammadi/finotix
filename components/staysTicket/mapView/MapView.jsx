import React from 'react';
import styles from "./mapView.module.css";

const MapView = () => {
    return (
        <button className={`w-full ${styles.mapViewContainer}`}>
            <div className={styles.mapViewStyles}>
                <div className={styles.mapViewBtn}>
aaa
                </div>
            </div>
        </button>
    );
};

export default MapView;
